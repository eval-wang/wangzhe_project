# 子项目3_数据定位 —— 需求对齐记录（2026-08-15）

## 任务描述

写一份 Python 脚本，放入 `D:\wangzhe_project\王者视频解析\子项目3_数据定位` 下：
将 `hero_grid_result.csv` 按 `hero_grid_result_with_symbol.csv` 的对应关系，
按"小地图位置"的映射关系，输出到基础表格中。

## 涉及文件

| 文件 | 路径 | 作用 |
| --- | --- | --- |
| hero_grid_result.csv | `D:\wangzhe_project\screenshot_collection\test_collection\test1\hero_grid_result.csv` | 采样数据（file, time_s, row, col, status, px, py, 符号） |
| hero_grid_result_with_symbol.csv | 同上目录 | 参照文件，结构与上面几乎一致 |
| 小地图位置.md | `D:\wangzhe_project\王者视频解析\子项目3_数据定位\小地图位置.md` | (行,列)→符号 的 20×20 网格映射表（内容为 CSV 格式） |
| 基础表格.xlsx | `D:\wangzhe_project\相关素材\表格\基础表格.xlsx` | 输出模板：列头 0:40 起每 90 秒一列（共 17 列），行头 0:15 起每 15 秒一行（共 6 行） |
| 参考表格.xlsx | `D:\wangzhe_project\相关素材\表格\参考表格.xlsx` | 最终结果的格式参照 |

## 疑点与确认答案

1. **文件名笔误**："hero_grid_result.scv" = `hero_grid_result.csv`；"hero_grid_result_wi.scv" = `hero_grid_result_with_symbol.csv`。
   → **确认：是。**

2. **"按照 with_symbol 的对应关系"的含义**：两个 CSV 内容几乎一致且都自带"符号"列（hero_grid_result.csv 的符号反而更全，多出 5 处）。
   → **确认：方案 (c)——两个 CSV 的符号列都不用，直接用 (row, col) 查"小地图位置"映射表现算符号，两个 CSV 仅作参照。**

3. **基础表格的格子语义**：列 90 秒一格、行 15 秒一格，6 行 × 15 秒 = 90 秒 = 一个列宽。
   → **确认：15 秒一个格子，一个格子记录 15 秒内发生的事件。整张表从 0:40 起按 15 秒一格连续铺开。**

4. **前 40 秒数据（0s/10s/20s/30s 四个采样点落在表格外）**：
   → **确认：丢弃。**

5. **一个 15 秒格子内有多个取样点**：
   → **确认：全部填入。**

6. **单元格填写内容**：
   → **确认：只填符号（如 r0、b2）。**

7. **empty 帧（status=empty，未识别到英雄）**：
   → **确认：填 "EM"。**

8. **输出方式**：
   → **确认：在 `子项目3_数据定位` 下另存一份结果文件，基础表格.xlsx 原件不动。**

9. **交付物**：
   → **确认：交付 .py 脚本，并运行生成结果表格。最终结果格式参照 `参考表格.xlsx`。**

## 补充背景（来自 项目3.md）

- 人物模型实际位置可能落在小地图未标注的格子上（(row, col) 查不到映射的情况存在，如 (1,3)、(8,10)）。
- 对抗路和发育路的位置每局不同，后续可能按对局方设计两种地图。

## 待确认问题（第二轮）—— 已全部确认

1. **多个符号的拼接格式**：参考表格中 `b02`、`r10` 这类值为拼接形式。
   → **确认：按时间顺序用下划线拼接，如 `b0_b1`。不去重。**

2. **EM 与其他符号同格时**：
   → **确认：同样按时间顺序用下划线拼接，如 `EM_b3`；整格都是 empty 时填 `EM`。**

3. **格子边界归属**：格子区间左闭右开，如第 1 列六行分别为 0:40–0:55、0:55–1:10、…、1:55–2:10。
   → **确认：恰好落在边界的采样点归入后一个格子。**

4. **(row, col) 查不到映射的格子**（小地图未标注点）：
   → **确认：填 `EM`。**

5. **列数与时间范围**：按基础表格的 17 列填充，参考表格仅作格式参照。
   → **确认：15:40 之后的格子全部留空。**

6. **输出文件名**：
   → **确认：`基础表格_填充结果.xlsx`，保存在 `子项目3_数据定位` 下。**

## 第三轮需求（2026-08-15 追加）—— 已确认并实施

1. **未标注点标记拆分**：原来未标注点和 empty 帧都填 `EM`。
   → **确认：未标注点（识别到英雄但 (row, col) 查不到映射）改填 `UN`；empty 帧仍填 `EM`。**

2. **UN / EM 事件导出功能**：在填充表格生成之后执行。
   → **确认：在 `子项目3_数据定位` 下分别创建 `UN/`、`EM/` 两个文件夹，各生成一份 CSV，记录事件出现的文件名称和次数；配套截图（与采样 CSV 同目录的 frame 图片）存在时一并复制进对应文件夹。**
   - `UN.csv` 列：file, time_s, row, col, count（按 文件+位置 聚合）
   - `EM.csv` 列：file, time_s, count（按文件聚合）
   - 只统计实际落入表格范围的采样点（time_s ≥ 40 且在 17 列内）。
   - 每次运行会先清空旧文件夹再生成，避免残留。

3. **文档沉淀**：
   → **确认：在 `子项目3_数据定位` 下写入 `SOP.md`（标准操作流程）和 `README.md`（项目说明），便于之后操作。**

## 第四轮调整（2026-08-15 追加）—— 已确认并实施

1. **产物与核心内容分离**：
   → **确认：运行产物（`基础表格_填充结果.xlsx`、`UN/`、`EM/`）输出到
   `D:\wangzhe_project\apply\document1\subproject3`；
   `王者视频解析\子项目3_数据定位` 下只保留脚本、映射表、SOP、README 等核心内容。**
   脚本 `OUTPUT_DIR` 已改为新目录，SOP / README 已同步更新。

## 第五轮调整（2026-08-15 追加）—— 已确认并实施

1. **产物命名思路**：确保最终结果可追踪、可复盘，最终结果按文件路径放置。
   → **确认：产物目录 = 产物根目录 + 阶段名（`<APPLY_ROOT>\<STAGE_NAME>\`，
   当前为 `apply\document1\subproject3`）；目标文件夹中的阶段文件夹（subproject3）
   由脚本自动创建，无需手动建立。**
   脚本路径配置改为 `APPLY_ROOT` + `STAGE_NAME` 两项，`main()` 起始处自动
   `mkdir(parents=True, exist_ok=True)`；已实测删除产物目录后重跑可自动重建。
   SOP、README 已同步该约定。

## 最终处理规则汇总

- 数据源：`hero_grid_result.csv` 的 (time_s, row, col, status)。
- 符号来源：仅用 (row, col) 查 `小地图位置.md` 映射表；两个 CSV 的符号列仅作参照，不使用。
- 丢弃 time_s < 40 的采样点。
- 格子 = 15 秒时间窗，左闭右开，边界点归入后一格。
- 单元格值：status=empty → `EM`；识别到但查不到映射 → `UN`；否则填符号；同格多个事件按时间顺序用 `_` 拼接。
- 结果另存为 `apply\document1\subproject3\基础表格_填充结果.xlsx`（第四轮起），原件不动。
